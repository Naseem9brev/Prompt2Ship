# Supabase

Supabase migrations and Auth configuration notes will live here.

The next focused PRs will add the Phase 1 tables, RLS policies, public leaderboard views, and GitHub OAuth implementation.

Phase 1 deployment settings, Supabase environment variable mapping, and the
GitHub OAuth callback URL matrix are documented in
[`../docs/deployment.md`](../docs/deployment.md).
