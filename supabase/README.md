# Supabase

Supabase migrations and Auth configuration notes will live here.

The next focused PR will add the Phase 1 tables, RLS policies, public leaderboard views, and GitHub OAuth setup documentation.
